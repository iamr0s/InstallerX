package com.rosan.installer.di.init

import com.rosan.installer.di.*

val appModules = listOf(
    roomModule,
    viewModelModule,
    protobufModule,
    appModule,
    resModule,
    workerModule,
    installerModule,
    reflectModule
)
